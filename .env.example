# Root workspace env example (used by local tooling and defaults)
# Express API runs in apps/api and also reads its own .env (see apps/api/.env.example)

# Common
NODE_ENV=development
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Database (Postgres)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=fuep
DB_PASSWORD=fuep
DB_NAME=fuep_portal
# Optional single URL override (if set, takes precedence)
DB_URL=

# Cookie/session (if needed by middleware)
COOKIE_SECRET=dev_cookie_secret_change

# MailHog (development mail)
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
# Branding (primary color used across docs and UI prototypes)
BRAND_PRIMARY_COLOR=FF134F47
