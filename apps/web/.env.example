# Vite frontend envs (these must start with VITE_)
# API base URL used by the Status page and other network calls
VITE_API_BASE_URL=http://localhost:4000

# Frontend brand color (CSS-valid). Defaults to #134F47 (derived from BRAND_PRIMARY_COLOR=FF134F47).
# If not set, the app falls back to #134F47.
VITE_BRAND_PRIMARY_COLOR=#134F47